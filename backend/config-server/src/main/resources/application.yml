server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/asma9028/RainIntel.git
          default-label: feature/person1-platform
          search-paths:
            - backend/config-repo